# DEV-JOURNAL — fox-hunter

## Статус
Игра «Охота на лис» опубликована. Проект рабочий, тесты зелёные.

## Деплой (2026-08-24)
- **GitHub-репозиторий:** https://github.com/it-ul/fox-hunter (публичный)
- **GitHub Pages (игра онлайн):** https://it-ul.github.io/fox-hunter/
  - Источник Pages: ветка `main`, папка `/docs` (автономная HTML/JS версия)
  - Flask-версия остаётся в репозитории для локального запуска (`python app.py`)
- **Как обновлять:** внести изменения → `git add -A` → `git commit -m "..."` →
  `git push origin main` (Pages пересоберётся автоматически)
- **Секреты:** `SECRET_KEY` задаётся переменной окружения (см. `.env.example`).

## Примечания
- Для push использован классический PAT пользователя (scope repo) — пользователю
  рекомендовано отозвать токен после завершения работ.
- Ветка локальная: `main` (переименована из `master`), upstream настроен на origin.
